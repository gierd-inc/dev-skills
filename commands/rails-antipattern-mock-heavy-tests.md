---
description: Use when tests rely on `mock(...)`, `stub(...)`, or `expect(...).to receive(...)` to fake out collaborators that could just be real objects backed by fixtures. The 37signals fixture-first stance.
---

<!-- auto-generated from skills/rails-antipatterns/rails-antipattern-mock-heavy-tests/SKILL.md -->

Invoke the `rails-antipattern-mock-heavy-tests` skill via the Skill tool, then apply it to the user's current task.
