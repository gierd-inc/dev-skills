---
description: Use when a model has a tangle of `before_*` / `after_*` callbacks that fire side effects (emails, jobs, external API calls, audit rows) on every save, making behavior unpredictable and tests slow.
---

<!-- auto-generated from skills/rails-antipatterns/rails-antipattern-callback-hell/SKILL.md -->

Invoke the `rails-antipattern-callback-hell` skill via the Skill tool, then apply it to the user's current task.
