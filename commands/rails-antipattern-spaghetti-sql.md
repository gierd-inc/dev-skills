---
description: Use when raw `where(...)` clauses, `find_by_sql`, or query fragments are scattered across controllers, views, and jobs instead of being encapsulated as model scopes. From *Rails Antipatterns*.
---

<!-- auto-generated from skills/rails-antipatterns/rails-antipattern-spaghetti-sql/SKILL.md -->

Invoke the `rails-antipattern-spaghetti-sql` skill via the Skill tool, then apply it to the user's current task.
