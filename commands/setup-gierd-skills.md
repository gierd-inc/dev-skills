---
description: Sets up an `## Agent skills` block in AGENTS.md/CLAUDE.md and `docs/agents/` so the engineering skills know this repo's issue tracker (GitHub, GitLab, Linear, or local markdown), triage label vocabulary, and domain doc layout. Run before first use of `to-issues`, `to-prd`, `triage`, `diagnose`, `tdd`, `improve-codebase-architecture`, or `zoom-out` — or if those skills appear to be missing context about the issue tracker, triage labels, or domain docs.
---

<!-- auto-generated from skills/engineering/setup-gierd-skills/SKILL.md -->

Invoke the `setup-gierd-skills` skill via the Skill tool, then apply it to the user's current task.
