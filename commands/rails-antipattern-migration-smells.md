---
description: Use when reviewing migrations that are irreversible, mix data backfills with schema changes, reference application models, or skip indexes on foreign keys. From *Rails Antipatterns*.
---

<!-- auto-generated from skills/rails-antipatterns/rails-antipattern-migration-smells/SKILL.md -->

Invoke the `rails-antipattern-migration-smells` skill via the Skill tool, then apply it to the user's current task.
