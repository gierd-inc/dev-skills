---
description: Use when controllers grow custom actions like `publish`, `archive`, `approve`, `notify_users` instead of introducing a new resource. DHH's "use REST" rule.
---

<!-- auto-generated from skills/rails-antipatterns/rails-antipattern-non-restful-actions/SKILL.md -->

Invoke the `rails-antipattern-non-restful-actions` skill via the Skill tool, then apply it to the user's current task.
