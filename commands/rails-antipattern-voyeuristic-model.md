---
description: Use when reviewing code that reaches across multiple objects to get at data — train wrecks like `order.customer.address.city` — violating the Law of Demeter. From *Rails Antipatterns* (Pytel & Saleh).
---

<!-- auto-generated from skills/rails-antipatterns/rails-antipattern-voyeuristic-model/SKILL.md -->

Invoke the `rails-antipattern-voyeuristic-model` skill via the Skill tool, then apply it to the user's current task.
